apiVersion: v1
kind: Service
metadata:
  name: fastapi
  namespace: app-ns
  labels:
    app: fastapi
spec:
  type: ClusterIP
  selector:
    app: fastapi
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
