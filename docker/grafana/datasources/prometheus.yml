# Grafana datasource provisioning — Prometheus
# Used by all scraper dashboards in the Real Estate Aggregation Platform.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
